#WebSocket (modular)

Defina un punto final WebSocket dentro de un controlador usando `async def SOCKET(self, websocket)`.

---

## Eco básico

```python
from nexy.decorators import Controller
from fastapi import WebSocket, WebSocketDisconnect

"@Controller("/chat")
class ChatController:
    async def SOCKET(self, websocket: WebSocket):
        await websocket.accept()
        try:
            while True:
                data = await websocket.receive_text()
                await websocket.send_text(f"Echo: {data}")
        except WebSocketDisconnect:
            pass
```

El nombre del método `SOCKET` se asigna a un punto final WebSocket. Todos los métodos en un controlador comparten el prefijo `"@Controller`, por lo que este es `GET /chat` como WebSocket.

---

## Recibir JSON

```python
from nexy.decorators import Controller
from fastapi import WebSocket, WebSocketDisconnect

"@Controller("/events")
class EventsController:
    async def SOCKET(self, websocket: WebSocket):
        await websocket.accept()
        try:
            while True:
                data = await websocket.receive_json()
                await websocket.send_json({"echo": data})
        except WebSocketDisconnect:
            pass
```

---

## Patrón de habitación con DI

Utilice un servicio dedicado para gestionar las conexiones:

```python
from nexy.decorators import Controller, Injectable
from fastapi import WebSocket, WebSocketDisconnect

"@Injectable()
class ConnectionManager:
    def __init__(self):
        self.connections: list[WebSocket] = []

    def add(self, ws: WebSocket):
        self.connections.append(ws)

    def remove(self, ws: WebSocket):
        self.connections.remove(ws)

    async def broadcast(self, message: str):
        for conn in self.connections:
            await conn.send_text(message)

"@Controller("/chat")
class ChatController:
    def __init__(self, manager: ConnectionManager):
        self.manager = manager

    async def SOCKET(self, websocket: WebSocket):
        await websocket.accept()
        self.manager.add(websocket)
        try:
            while True:
                data = await websocket.receive_text()
                await self.manager.broadcast(data)
        except WebSocketDisconnect:
            self.manager.remove(websocket)
```

`ConnectionManager` es un singleton `"@Injectable()`: todas las instancias del controlador comparten el mismo administrador.

---

## Habitaciones con nombre con DI

```python
from nexy.decorators import Controller, Injectable
from fastapi import WebSocket, WebSocketDisconnect

"@Injectable()
class RoomManager:
    def __init__(self):
        self.rooms: dict[str, list[WebSocket]] = {}

    def join(self, room: str, ws: WebSocket):
        self.rooms.setdefault(room, []).append(ws)

    def leave(self, room: str, ws: WebSocket):
        self.rooms[room].remove(ws)
        if not self.rooms[room]:
            del self.rooms[room]

    async def broadcast(self, room: str, message: str):
        for conn in self.rooms.get(room, []):
            await conn.send_text(message)

"@Controller("/rooms/{room_id}")
class RoomController:
    def __init__(self, rooms: RoomManager):
        self.rooms = rooms

    async def SOCKET(self, websocket: WebSocket, room_id: str):
        await websocket.accept()
        self.rooms.join(room_id, websocket)
        try:
            while True:
                data = await websocket.receive_text()
                await self.rooms.broadcast(room_id, data)
        except WebSocketDisconnect:
            self.rooms.leave(room_id, websocket)
```

---

## Ciclo de vida de la conexión

| Evento | ¿Qué pasa?
|-------|-------------|
| El cliente se conecta | `websocket.accept()` — debe llamarse primero |
| Mensaje recibido | `receive_text()` / `receive_json()` / `receive_bytes()` |
| El cliente se desconecta | `WebSocketDisconnect` levantado — limpiar en `except` |
| El servidor se cierra | `await websocket.close(code=1000)` |

---

## Cliente

```javascript
const ws = new WebSocket("ws://localhost:3000/chat");
ws.onmessage = (event) => console.log(event.data);
ws.send("Hello from the client!");
```

---

## Ver también

- [Controllers](/docs/modular/controllers) — controladores de ruta en Modular
- [Providers & DI](/docs/modular/providers) — servicios inyectables
- [Guards](/docs/modular/guards): protege los puntos finales de WebSocket
- [WebSocket Guide](/docs/guides/websocket) — descripción general conceptual
