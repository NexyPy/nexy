# WebSocket (modulaire)

Définissez un point de terminaison WebSocket à l'intérieur d'un contrôleur à l'aide de `async def SOCKET(self, websocket)`.

---

## Écho de base

```python
from nexy.decorators import Controller
from fastapi import WebSocket, WebSocketDisconnect

"@Controller("/chat")
class ChatController:
    async def SOCKET(self, websocket: WebSocket):
        await websocket.accept()
        try:
            while True:
                data = await websocket.receive_text()
                await websocket.send_text(f"Echo: {data}")
        except WebSocketDisconnect:
            pass
```

Le nom de la méthode `SOCKET` est mappé à un point de terminaison WebSocket. Toutes les méthodes d'un contrôleur partagent le préfixe `"@Controller` — il s'agit donc de `GET /chat` en tant que WebSocket.

---

## Recevoir JSON

```python
from nexy.decorators import Controller
from fastapi import WebSocket, WebSocketDisconnect

"@Controller("/events")
class EventsController:
    async def SOCKET(self, websocket: WebSocket):
        await websocket.accept()
        try:
            while True:
                data = await websocket.receive_json()
                await websocket.send_json({"echo": data})
        except WebSocketDisconnect:
            pass
```

---

## Modèle de pièce avec DI

Utilisez un service dédié pour gérer les connexions :

```python
from nexy.decorators import Controller, Injectable
from fastapi import WebSocket, WebSocketDisconnect

"@Injectable()
class ConnectionManager:
    def __init__(self):
        self.connections: list[WebSocket] = []

    def add(self, ws: WebSocket):
        self.connections.append(ws)

    def remove(self, ws: WebSocket):
        self.connections.remove(ws)

    async def broadcast(self, message: str):
        for conn in self.connections:
            await conn.send_text(message)

"@Controller("/chat")
class ChatController:
    def __init__(self, manager: ConnectionManager):
        self.manager = manager

    async def SOCKET(self, websocket: WebSocket):
        await websocket.accept()
        self.manager.add(websocket)
        try:
            while True:
                data = await websocket.receive_text()
                await self.manager.broadcast(data)
        except WebSocketDisconnect:
            self.manager.remove(websocket)
```

Le `ConnectionManager` est un singleton `"@Injectable()` — toutes les instances de contrôleur partagent le même gestionnaire.

---

## Pièces nommées avec DI

```python
from nexy.decorators import Controller, Injectable
from fastapi import WebSocket, WebSocketDisconnect

"@Injectable()
class RoomManager:
    def __init__(self):
        self.rooms: dict[str, list[WebSocket]] = {}

    def join(self, room: str, ws: WebSocket):
        self.rooms.setdefault(room, []).append(ws)

    def leave(self, room: str, ws: WebSocket):
        self.rooms[room].remove(ws)
        if not self.rooms[room]:
            del self.rooms[room]

    async def broadcast(self, room: str, message: str):
        for conn in self.rooms.get(room, []):
            await conn.send_text(message)

"@Controller("/rooms/{room_id}")
class RoomController:
    def __init__(self, rooms: RoomManager):
        self.rooms = rooms

    async def SOCKET(self, websocket: WebSocket, room_id: str):
        await websocket.accept()
        self.rooms.join(room_id, websocket)
        try:
            while True:
                data = await websocket.receive_text()
                await self.rooms.broadcast(room_id, data)
        except WebSocketDisconnect:
            self.rooms.leave(room_id, websocket)
```

---

## Cycle de vie de la connexion

| Événement | Que se passe-t-il |
|-------|-------------|
| Le client se connecte | `websocket.accept()` — doit être appelé en premier |
| Message reçu | `receive_text()` / `receive_json()` / `receive_bytes()` |
| Le client se déconnecte | `WebSocketDisconnect` augmenté — nettoyage dans `except` |
| Le serveur se ferme | `await websocket.close(code=1000)` |

---

## Client

```javascript
const ws = new WebSocket("ws://localhost:3000/chat");
ws.onmessage = (event) => console.log(event.data);
ws.send("Hello from the client!");
```

---

## Voir aussi

- [Controllers](/docs/modular/controllers) — gestionnaires de routes dans Modular
- [Providers & DI](/docs/modular/providers) — services injectables
- [Guards](/docs/modular/guards) — protège les points de terminaison WebSocket
- [WebSocket Guide](/docs/guides/websocket) — aperçu conceptuel
