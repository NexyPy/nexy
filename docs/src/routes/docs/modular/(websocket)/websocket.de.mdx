# WebSocket (modular)

Definieren Sie einen WebSocket-Endpunkt innerhalb eines Controllers mit `async def SOCKET(self, websocket)`.

---

## Grundlegendes Echo

```python
from nexy.decorators import Controller
from fastapi import WebSocket, WebSocketDisconnect

"@Controller("/chat")
class ChatController:
    async def SOCKET(self, websocket: WebSocket):
        await websocket.accept()
        try:
            while True:
                data = await websocket.receive_text()
                await websocket.send_text(f"Echo: {data}")
        except WebSocketDisconnect:
            pass
```

Der Methodenname `SOCKET` wird einem WebSocket-Endpunkt zugeordnet. Alle Methoden in einem Controller teilen sich das Präfix `"@Controller` – dies ist also `GET /chat` als WebSocket.

---

## JSON empfangen

```python
from nexy.decorators import Controller
from fastapi import WebSocket, WebSocketDisconnect

"@Controller("/events")
class EventsController:
    async def SOCKET(self, websocket: WebSocket):
        await websocket.accept()
        try:
            while True:
                data = await websocket.receive_json()
                await websocket.send_json({"echo": data})
        except WebSocketDisconnect:
            pass
```

---

## Raummuster mit DI

Verwenden Sie einen dedizierten Dienst, um Verbindungen zu verwalten:

```python
from nexy.decorators import Controller, Injectable
from fastapi import WebSocket, WebSocketDisconnect

"@Injectable()
class ConnectionManager:
    def __init__(self):
        self.connections: list[WebSocket] = []

    def add(self, ws: WebSocket):
        self.connections.append(ws)

    def remove(self, ws: WebSocket):
        self.connections.remove(ws)

    async def broadcast(self, message: str):
        for conn in self.connections:
            await conn.send_text(message)

"@Controller("/chat")
class ChatController:
    def __init__(self, manager: ConnectionManager):
        self.manager = manager

    async def SOCKET(self, websocket: WebSocket):
        await websocket.accept()
        self.manager.add(websocket)
        try:
            while True:
                data = await websocket.receive_text()
                await self.manager.broadcast(data)
        except WebSocketDisconnect:
            self.manager.remove(websocket)
```

Der `ConnectionManager` ist ein Singleton `"@Injectable()` – alle Controller-Instanzen nutzen denselben Manager.

---

## Benannte Räume mit DI

```python
from nexy.decorators import Controller, Injectable
from fastapi import WebSocket, WebSocketDisconnect

"@Injectable()
class RoomManager:
    def __init__(self):
        self.rooms: dict[str, list[WebSocket]] = {}

    def join(self, room: str, ws: WebSocket):
        self.rooms.setdefault(room, []).append(ws)

    def leave(self, room: str, ws: WebSocket):
        self.rooms[room].remove(ws)
        if not self.rooms[room]:
            del self.rooms[room]

    async def broadcast(self, room: str, message: str):
        for conn in self.rooms.get(room, []):
            await conn.send_text(message)

"@Controller("/rooms/{room_id}")
class RoomController:
    def __init__(self, rooms: RoomManager):
        self.rooms = rooms

    async def SOCKET(self, websocket: WebSocket, room_id: str):
        await websocket.accept()
        self.rooms.join(room_id, websocket)
        try:
            while True:
                data = await websocket.receive_text()
                await self.rooms.broadcast(room_id, data)
        except WebSocketDisconnect:
            self.rooms.leave(room_id, websocket)
```

---

## Verbindungslebenszyklus

| Veranstaltung | Was passiert |
|-------|-------------|
| Client verbindet | `websocket.accept()` – muss zuerst aufgerufen werden |
| Nachricht erhalten | `receive_text()` / `receive_json()` / `receive_bytes()` |
| Client trennt die Verbindung | `WebSocketDisconnect` erhöht – Aufräumen in `except` |
| Server wird geschlossen | `await websocket.close(code=1000)` |

---

## Kunde

```javascript
const ws = new WebSocket("ws://localhost:3000/chat");
ws.onmessage = (event) => console.log(event.data);
ws.send("Hello from the client!");
```

---

## Siehe auch

- [Controllers](/docs/modular/controllers) – Routenhandler in Modular
- [Providers & DI](/docs/modular/providers) – injizierbare Dienstleistungen
- [Guards](/docs/modular/guards) – WebSocket-Endpunkte schützen
- [WebSocket Guide](/docs/guides/websocket) – konzeptioneller Überblick
