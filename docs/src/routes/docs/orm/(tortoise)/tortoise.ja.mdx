# Tortoise-ORM

**Tortoise-ORM** は、Django の ORM からインスピレーションを得た非同期ネイティブ ORM です。習得が簡単で、FastAPI/Nexy とうまく連携します。

＃＃ 構成

```python
TORTOISE_ORM = {
    "connections": {"default": "sqlite://dev.db"},
    "apps": {"models": {"models": ["models", "aerich.models"], "default_connection": "default"}},
}
```

＃＃ 例

```python
from tortoise import fields
from tortoise.models import Model

class User(Model):
    id = fields.IntField(pk=True)
    name = fields.CharField(max_length=100)
    email = fields.CharField(max_length=100)
```

## 移行

Tortoise-ORM は **Aerich** を使用します (`nx migrate` によって構成):

```bash
nx migrate --init    # Initialize Aerich (first time)
nx migrate           # Create migration
nx migrate --upgrade # Apply
```

Tortoise-ORM は、Django から来ていて、非同期サポートを備えた使い慣れた API が必要な場合に最適です。
