# रिस्पांस मॉडल (एफबीआर)

प्रतिक्रिया क्रमांकन, स्थिति कोड और प्रतिक्रिया प्रकार को नियंत्रित करें।

## पाइडेंटिक प्रतिक्रिया मॉडल

```python
# src/routes/users.py
from pydantic import BaseModel

class UserOut(BaseModel):
    id: int
    name: str
    email: str

def GET() -> UserOut:
    return UserOut(id=1, name="Alice", email="alice"@example.com")
```

## सूची प्रतिक्रिया

```python
def GET() -> list[UserOut]:
    return [UserOut(id=1, name="Alice")]
```

## प्रतिक्रिया प्रकार

`fastapi.responses` से आयात:

```python
from fastapi.responses import (
    JSONResponse,
    HTMLResponse,
    PlainTextResponse,
    RedirectResponse,
    StreamingResponse,
    FileResponse,
    ORJSONResponse,
)
```

### HTMLप्रतिक्रिया

```python
from fastapi.responses import HTMLResponse

def GET() -> HTMLResponse:
    return HTMLResponse("<h1>Hello</h1>")
```

### स्ट्रीमिंगरिस्पॉन्स

```python
from fastapi.responses import StreamingResponse
import io

def GET() -> StreamingResponse:
    return StreamingResponse(io.StringIO("large CSV data..."), media_type="text/csv")
```

## कस्टम स्थिति के साथ सीधी प्रतिक्रिया

```python
from fastapi.responses import JSONResponse

def GET() -> JSONResponse:
    return JSONResponse(
        content={"msg": "created"},
        status_code=201,
        headers={"X-Custom": "value"},
    )
```

## समर्थित प्रतिक्रिया प्रकार

| क्लास | सामग्री-प्रकार | उपयोग का मामला |
|-------|----|---|
| `JSONResponse` | `application/json` | JSON डेटा (डिफ़ॉल्ट) |
| `HTMLResponse` | `text/html` | HTML स्ट्रिंग्स |
| `PlainTextResponse` | `text/plain` | कच्चा पाठ |
| `RedirectResponse` | — | रीडायरेक्ट |
| `StreamingResponse` | बदलता रहता है | स्ट्रीम डेटा |
| `FileResponse` | बदलता रहता है | फ़ाइल डाउनलोड |
| `ORJSONResponse` | `application/json` | तेज़ JSON (`orjson` की आवश्यकता है) |
