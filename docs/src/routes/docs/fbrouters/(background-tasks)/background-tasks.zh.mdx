# 后台任务 (FBR)

*在*发送响应后运行代码——用于日志记录、通知、缓存预热。

```python
from fastapi import BackgroundTasks
```

## 单任务

```python
# src/routes/log.py
from fastapi import BackgroundTasks

def write_log(message: str):
    with open("app.log", "a") as f:
        f.write(message + "\n")

def GET(tasks: BackgroundTasks):
    tasks.add_task(write_log, "Page visited")
    return {"status": "ok"}
```

## 多个任务

```python
from fastapi import BackgroundTasks

def GET(tasks: BackgroundTasks):
    tasks.add_task(log_visit)
    tasks.add_task(notify_admin)
    tasks.add_task(warm_cache)
    return {"ok": True}
```

## 具有依赖关系的任务

```python
from fastapi import BackgroundTasks, Depends
from src.db import get_db

def cleanup_old_records(db=Depends(get_db)):
    db.query(Log).delete()

def GET(tasks: BackgroundTasks):
    tasks.add_task(cleanup_old_records)
    return {"status": "cleaning"}
```

## 重要

- 任务在发送响应后**运行 - 客户端不等待
- 它们在同一个进程中运行 - 长任务会阻塞事件循环
- 对于繁重的工作，使用任务队列（Celery、ARQ、RQ 等）
- 任务中的错误会被悄悄吞掉——在任务函数中添加 try/ except
- 任务接收可变对象的**副本**——突变不会影响响应
