# 服务器操作

服务器操作允许您从客户端调用 Python 函数作为 POST 端点 - 无需路由注册，无需序列化样板。

---

## 定义一个动作

用 `"@Action` 修饰任何函数：

```python
# src/actions/vote.py
from nexy.decorators import Action

"@Action
def vote(post_id: str, direction: int) -> dict:
    return {"success": True, "post_id": post_id, "votes": 42}
```

该函数接收 JSON 主体参数作为关键字参数。返回任何 JSON 可序列化值。

---

## 动作文件放在哪里

操作可以存在于项目中的 **任何地方** — `src/actions/`、`src/routes/` 或直接位于根目录中。 `ActionEngine` 在启动时扫描整个项目目录。

```text
my-app/
├── src/
│   └── actions/
│       ├── vote.py        ← "@Action
│       └── comment.py     ← "@Action
├── actions/
│   ├── vote.ts            ← Generated TypeScript client
│   └── comment.ts
```

请参阅 ["@Action reference](/docs/decorators/action) 了解发现和端点生成的工作原理。

---

## 实际例子

### 表单提交

```python
# src/actions/contact.py
from nexy.decorators import Action
from pydantic import BaseModel, EmailStr

class ContactForm(BaseModel):
    name: str
    email: EmailStr
    message: str

"@Action
def submit_contact(data: ContactForm) -> dict:
    # Save to database, send email, etc.
    return {"ok": True, "name": data.name}
```

```nexy
---
---
<form id="contact">
  <input name="name" required />
  <input name="email" type="email" required />
  <textarea name="message" required></textarea>
  <button type="submit">Send</button>
</form>

<script>
document.getElementById('contact').onsubmit = async (e) => {
  e.preventDefault();
  const fd = new FormData(e.target);
  const res = await fetch('/_nx_a1b2c3d4e5f6', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(Object.fromEntries(fd))
  });
  const data = await res.json();
  if (data.ok) alert('Message sent!');
};
</script>
```

### 数据库操作

```python
# src/actions/create_user.py
from nexy.decorators import Action
from pydantic import BaseModel
from src.database import get_session
from src.models import User

class CreateUserBody(BaseModel):
    name: str
    email: str

"@Action
def create_user(body: CreateUserBody) -> dict:
    db = next(get_session())
    user = User(name=body.name, email=body.email)
    db.add(user)
    db.commit()
    db.refresh(user)
    return {"id": user.id, "name": user.name}
```

### 带有错误处理的操作

```python
# src/actions/delete_item.py
from nexy.decorators import Action
from fastapi import HTTPException
from src.database import get_session

"@Action
def delete_item(item_id: int) -> dict:
    db = next(get_session())
    item = db.get(Item, item_id)
    if not item:
        raise HTTPException(status_code=404, detail="Item not found")
    db.delete(item)
    db.commit()
    return {"deleted": item_id}
```

---

## 生成的 TypeScript 客户端

`nx build` 或 `nx dev` 在 `./actions/` 中生成类型安全的 TypeScript 包装器：

```typescript
// Generated: ./actions/vote.ts
import { transport } from '"@/nexy/client';

export const vote = async (post_id: string, direction: number): Promise<dict> => {
  return await transport.call('/_nx_a1b2c3d4e5f6', { post_id, direction });
};
```

每个生成的函数都使用正确的 TypeScript 类型来镜像 Python 签名。

---

## 最佳实践

- 保持动作函数集中且幂等
- 验证操作内的输入 - 将它们视为不可信
- 使用 Pydantic 模型处理复杂的请求体
- 操作仅限 POST — 对于其他 HTTP 方法，请使用路由处理程序
- 使用警卫或中间件进行授权

---

## 参考

请参阅 ["@Action decorator](/docs/decorators/action) 了解完整的 API 参考（参数类型、返回类型、限制）。
