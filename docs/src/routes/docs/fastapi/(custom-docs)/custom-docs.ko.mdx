# 사용자 정의 문서 UI

Nexy는 기본적으로 Swagger UI와 ReDoc을 사용합니다. 사용자 정의하거나 자체 호스팅하세요.

## Swagger UI 구성

```python
from nexy import app

app.swagger_ui_parameters = {
    "docExpansion": "none",        # collapse all sections
    "defaultModelsExpandDepth": -1, # hide schemas section
    "tryItOutEnabled": True,
}
```

## 자체 호스트 Swagger UI

```python
from nexy import app
from fastapi.openapi.docs import (
    get_swagger_ui_html,
    get_swagger_ui_oauth2_redirect_html,
)

"@app.get("/docs", include_in_schema=False)
async def custom_swagger_ui():
    return get_swagger_ui_html(
        openapi_url=app.openapi_url,
        title="My API",
        swagger_js_url="/static/swagger-ui-bundle.js",
        swagger_css_url="/static/swagger-ui.css",
    )
```

## 조건부 OpenAPI

프로덕션에서 OpenAPI를 비활성화합니다.

```python
import os

"@app.get("/openapi.json", include_in_schema=False)
async def openapi():
    if os.getenv("ENV") == "production":
        return {"openapi": "3.1.0", "info": {"title": "My API", "version": "1.0.0"}}
```

## 문서 URL 제어

[nexyconfig.py](/docs/config/nexy)에서 문서 엔드포인트를 구성합니다. `useDocs`, `useDocsUrl`, `useRedocsUrl` 필드를 참조하세요.
