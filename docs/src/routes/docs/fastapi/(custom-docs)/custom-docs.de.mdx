# Benutzeroberfläche für benutzerdefinierte Dokumente

Nexy verwendet standardmäßig Swagger UI und ReDoc. Passen Sie sie an oder hosten Sie sie selbst.

## Konfigurieren Sie die Swagger-Benutzeroberfläche

```python
from nexy import app

app.swagger_ui_parameters = {
    "docExpansion": "none",        # collapse all sections
    "defaultModelsExpandDepth": -1, # hide schemas section
    "tryItOutEnabled": True,
}
```

## Selbsthostende Swagger-Benutzeroberfläche

```python
from nexy import app
from fastapi.openapi.docs import (
    get_swagger_ui_html,
    get_swagger_ui_oauth2_redirect_html,
)

"@app.get("/docs", include_in_schema=False)
async def custom_swagger_ui():
    return get_swagger_ui_html(
        openapi_url=app.openapi_url,
        title="My API",
        swagger_js_url="/static/swagger-ui-bundle.js",
        swagger_css_url="/static/swagger-ui.css",
    )
```

## Bedingte OpenAPI

Deaktivieren Sie OpenAPI in der Produktion:

```python
import os

"@app.get("/openapi.json", include_in_schema=False)
async def openapi():
    if os.getenv("ENV") == "production":
        return {"openapi": "3.1.0", "info": {"title": "My API", "version": "1.0.0"}}
```

## Dokumenten-URLs steuern

Konfigurieren Sie Dokumentenendpunkte in [nexyconfig.py](/docs/config/nexy) – siehe Felder `useDocs`, `useDocsUrl`, `useRedocsUrl`.
