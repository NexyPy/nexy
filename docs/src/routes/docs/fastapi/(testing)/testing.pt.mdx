# Teste

Os aplicativos Nexy são aplicativos FastAPI subjacentes - teste-os com `TestClient` da Starlette.

```bash
pip install httpx pytest-anyio  # httpx for TestClient, anyio for async tests
```

## Configuração de teste

```python
# tests/test_app.py
from fastapi.testclient import TestClient
from nexy.app import app

client = TestClient(app)

def test_home():
    response = client.get("/")
    assert response.status_code == 200
    assert response.json() == {"Hello": "World"}
```

## Testando rotas

```python
def test_create_user():
    response = client.post("/users", json={"name": "Alice", "email": "a"@b.com"})
    assert response.status_code == 201
    data = response.json()
    assert data["name"] == "Alice"
```

## Testando com autenticação

```python
def test_protected_route():
    response = client.get("/admin", headers={"Authorization": "Bearer test-token"})
    assert response.status_code == 200
```

## Jogos com pytest

```python
# conftest.py
import pytest
from fastapi.testclient import TestClient
from nexy.app import app

"@pytest.fixture
def client():
    return TestClient(app)

# test_users.py
def test_list_users(client):
    resp = client.get("/users")
    assert resp.status_code == 200
```

## Testando WebSocket

```python
def test_ws():
    with client.websocket_connect("/ws") as ws:
        ws.send_json({"type": "ping"})
        data = ws.receive_json()
        assert data["type"] == "pong"
```

## Testes assíncronos

```python
import pytest

"@pytest.mark.anyio
async def test_async_route():
    async with AsyncClient(app=app, base_url="http://test") as ac:
        response = await ac.get("/")
        assert response.status_code == 200
```

## Dicas

- Use `TestClient` como gerenciador de contexto se seu aplicativo tiver eventos de vida útil
- Simulação de serviços externos com `httpx.MockTransport` ou `responses`
- Execute testes com `pytest tests/ -v` (nenhum comando Nexy especial é necessário)
- O TestClient usa `httpx` internamente — mesma API que `requests`
