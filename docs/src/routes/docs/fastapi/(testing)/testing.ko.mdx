# 테스트

Nexy 앱은 내부적으로 FastAPI 앱입니다. Starlette의 `TestClient`로 테스트해 보세요.

```bash
pip install httpx pytest-anyio  # httpx for TestClient, anyio for async tests
```

## 테스트 설정

```python
# tests/test_app.py
from fastapi.testclient import TestClient
from nexy.app import app

client = TestClient(app)

def test_home():
    response = client.get("/")
    assert response.status_code == 200
    assert response.json() == {"Hello": "World"}
```

## 테스트 경로

```python
def test_create_user():
    response = client.post("/users", json={"name": "Alice", "email": "a"@b.com"})
    assert response.status_code == 201
    data = response.json()
    assert data["name"] == "Alice"
```

## 인증으로 테스트하기

```python
def test_protected_route():
    response = client.get("/admin", headers={"Authorization": "Bearer test-token"})
    assert response.status_code == 200
```

## pytest를 사용한 설비

```python
# conftest.py
import pytest
from fastapi.testclient import TestClient
from nexy.app import app

"@pytest.fixture
def client():
    return TestClient(app)

# test_users.py
def test_list_users(client):
    resp = client.get("/users")
    assert resp.status_code == 200
```

## 웹소켓 테스트하기

```python
def test_ws():
    with client.websocket_connect("/ws") as ws:
        ws.send_json({"type": "ping"})
        data = ws.receive_json()
        assert data["type"] == "pong"
```

## 비동기 테스트

```python
import pytest

"@pytest.mark.anyio
async def test_async_route():
    async with AsyncClient(app=app, base_url="http://test") as ac:
        response = await ac.get("/")
        assert response.status_code == 200
```

## 팁

- 앱에 수명 이벤트가 있는 경우 `TestClient`를 컨텍스트 관리자로 사용하세요.
- `httpx.MockTransport` 또는 `responses`로 외부 서비스를 모의합니다.
- `pytest tests/ -v`로 테스트 실행(특별한 Nexy 명령이 필요하지 않음)
- TestClient는 내부적으로 `httpx`를 사용합니다 — `requests`와 동일한 API
