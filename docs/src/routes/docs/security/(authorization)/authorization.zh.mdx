# 授权

使用 `"@UseGuard` 装饰器和防护可调用对象来保护路由。

## 守卫合同

守卫可以用 `__call__(self, request)` 调用。提高 `HTTPException` 阻止，返回 `None` 允许。
```python
from fastapi import HTTPException

class AdminGuard:
    def __call__(self, request):
        if not request.user.is_authenticated:
            raise HTTPException(401)
        if "admin" not in request.user.scopes:
            raise HTTPException(403)
```
---

## 守卫路线
```python
---
from nexy.decorators import UseGuard
from starlette.authentication import requires

"@UseGuard(requires("authenticated"))
def GET(request):
    return {"user": request.user.username}
---
```
```python
from nexy.decorators import Controller, UseGuard
from starlette.authentication import requires

"@Controller("/admin")
"@UseGuard(requires("authenticated"))
class AdminController:
    def GET(self, request):
        return {"user": request.user.username}
```
## 多个守卫
```python
---
from nexy.decorators import UseGuard

"@UseGuard(AuthGuard(), RateLimitGuard())
def GET():
    ...
---
```
```python
"@UseGuard(AuthGuard(), RoleGuard("admin"))
class AdminController:
    def GET(self):
        ...
```
## 使用来自 Starlette 的 `requires`
```python
from starlette.authentication import requires

"@UseGuard(requires("authenticated"))
def GET(request):
    return {"user": request.user.username}
```
适用于 FBR 和模块化 — `requires` 返回一个有效的可调用守卫。

---

## 相关

- ["@UseGuard reference](/docs/decorators/useguard) — 完整的装饰器 API
- [Guards (Modular)](/docs/modular/guards) — 模块化 DI 中的保护注入
- [Authentication](/docs/security/authentication) — 设置身份验证后端