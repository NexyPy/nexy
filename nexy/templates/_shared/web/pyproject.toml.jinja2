[project]
name = "{{ project_name }}"
version = "0.1.0"
description = "A Nexy web application with {{ client_framework or 'vanilla' }} components"
dependencies = [
    "nexy>=2.0.8",
]
requires-python = ">=3.12"
